{
  "name": "rollorian-book-archive-backend",
  "version": "0.1.0",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "npm run db:push -- --skip-generate && node --test",
    "smoke:responsive": "node scripts/responsive-smoke.js",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "playwright-core": "^1.58.2",
    "prisma": "^6.6.0",
    "supertest": "^7.2.2"
  }
}
